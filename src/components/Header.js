const Header = ({ style }) => {
    return (
        <div
            className="fw-bold fs-3 bg-primary text-white text-center d-flex align-items-center justify-content-center"
            style={style}>
            My Panda
        </div>
    );
}
export default Header;